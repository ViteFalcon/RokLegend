# Sources
SET(ROKLEGEND_SOURCES
    CMakeLists.txt
    RokLegend.cpp
)

#IF (WIN32)
#    SET(APP_TYPE WIN32)
#ENDIF ()

ADD_EXECUTABLE(${ROKLEGEND_PROJECT_NAME} ${APP_TYPE} ${ROKLEGEND_SOURCES})
TARGET_LINK_LIBRARIES(${ROKLEGEND_PROJECT_NAME}
    debug ${ROKLEGEND_LIB_DIR_DBG}/${RO_CORE_LIBRARY}_d.lib optimized ${ROKLEGEND_LIB_DIR_DBG}/${RO_CORE_LIBRARY}.lib
)